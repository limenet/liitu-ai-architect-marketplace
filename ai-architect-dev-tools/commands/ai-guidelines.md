---
description: Create a project-level implementation guidelines directory (docs/guidelines/)
---

Use the `guidelines` skill from the `ai-architect-dev-tools` plugin to scan the codebase for UI component, styling, structure, and naming conventions, ask the user about rules not visible in code, and write a binding `docs/guidelines/` directory (one file per chapter plus a `README.md` index).

$ARGUMENTS
