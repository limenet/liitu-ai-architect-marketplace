---
description: Create a project-level implementation guidelines document (docs/guidelines.md)
---

Use the `guidelines` skill from the `ai-architect-dev-tools` plugin to scan the codebase for UI component, styling, structure, and naming conventions, ask the user about rules not visible in code, and write a binding `docs/guidelines.md`.

$ARGUMENTS
